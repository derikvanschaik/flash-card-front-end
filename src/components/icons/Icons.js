import {FcNext, FcPrevious, FcRedo, FcCancel} from 'react-icons/fc';
import { FaEdit, FaTrash } from "react-icons/fa";
import { MdLibraryAdd } from "react-icons/md";

//  Where we store icons for the application and export them to the application 

const NextIcon = FcNext;
const PreviousIcon = FcPrevious;
const FlipIcon = FcRedo;
const CancelIcon = FcCancel;

const EditIcon = FaEdit;
const DeleteIcon = FaTrash;

const AddIcon = MdLibraryAdd;

export {
    NextIcon,
    PreviousIcon,
    FlipIcon,
    CancelIcon,
    EditIcon,
    DeleteIcon,
    AddIcon,
}

